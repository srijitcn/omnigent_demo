# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# macOS
.DS_Store

# Omnigent local state / logs (if any land in the app dir)
.omnigent/

# Agent-generated demo OUTPUTS — regenerate by running ontogent. Excluded to
# keep the repo to the orchestrator itself and to avoid committing
# workspace-specific artifacts (e.g. a hardcoded workspace host in generated code).
demo1/
books_app/
